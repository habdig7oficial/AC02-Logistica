import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AC02-Logistica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
